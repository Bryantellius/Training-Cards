import * as React from "react";

const Footer: React.FC<IFooterProps> = () => {
  return (
    <main className="container my-5">
      <h1 className="text-primary text-center">Footer</h1>
    </main>
  );
};

export interface IFooterProps {}

export default Footer;
